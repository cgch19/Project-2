<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <h1>Contact List</h1>
    <h2><a class="contact-link" href="/contact/new">Add a New Contact</a></h2>
    <ul class="contacts">
        <% for(let i = 0; i < allContacts.length; i++) { %>
            <li>
                <h3><a class="contact-name" href="/contact/<%= i %>">
                    <%= allContacts[i].name %>
                </a></h3>
                <li>
                <a class="edit-contact" href="/contact/<%= i %>/edit" id="index-edit-btn">Edit Contact</a>
                </li>
                <form action="/contact/<%= i %>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete" id="button">
                </form>
            </li>
            
        <% } %>
    </ul>
</body>
</html>