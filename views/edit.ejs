<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <h1 class="form-heading">Edit Contact</h1>
    <form action="/contacts/<%= contact.id %>?_method=PUT" method="POST" class="contact-form">
        <div class="form-group">
            <label for="imageUrl" class="form-label">Image URL:</label>
            <input type="text" id="imageUrl" name="imageUrl" class="form-input" placeholder="Enter URL"/>
        </div>
        <div class="form-group">
            <label for="name" class="form-label">Name</label>
            <input value="<%= contact.name %>" class="form-input" type="text" name="name"/>
        </div>
        <div class="form-group">
            <label for="phone" class="form-label">Phone Number</label>
            <input value="<%= contact.phone %>" class="form-input" type="text" name="phone" id="phone"/>
        </div>
        <div class="form-group">
            <label for="address" class="form-label">Address</label>
            <input value="<%= contact.address %>" class="form-input" type="text" name="address" id="address"/>
        </div>
        <div class="button-container">
            <input type="submit" class="btn btn-primary" value="Submit"/>
            <a href="/contacts" class="btn-sign-out">Home</a>
    </form>     
    <%- include ("./partials/footer.ejs") %>
</body>
</html>