<header>
    <div class="container">
        <% if (currentUser) { %>
            <li class="nav-link">Welcome <%= currentUser.usernamee %></li>
        <% } else { %>
            <li class="nav-link">Welcome Guest</li>
        <% } %>
    </div>
</header>